<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.show.user.dao.SysUserDao">
	<select id="queryUserList" resultType="java.util.Map">
		select * from t_sys_user
	</select>

	<select id="getLoginUser" resultType="java.util.Map">
		select * from t_sys_user u where u.user_name = #{userName} and u.user_passwd = #{passWord}
	</select>

</mapper>